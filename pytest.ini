[pytest]
markers =
    live_llm: tests that call a real OpenAI-compatible LLM endpoint
    full_loop: tests that cover create-confirm-execute cycles without external services
    live_full_loop: tests that cover create-confirm-execute cycles with the parent project LLM capability
